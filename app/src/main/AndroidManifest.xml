<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ChatApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatApp"
        tools:targetApi="31">
        <activity
            android:name=".views.activities.ShowAllUsersActivity"
            android:exported="false" />
        <activity
            android:name=".views.activities.CreateGroupActivity"
            android:exported="false" />
        <activity
            android:name=".views.activities.ProfileInfoActivity"
            android:exported="false" />
        <activity
            android:name=".views.activities.ProfileSetupActivity"
            android:exported="false" />
        <activity
            android:name=".views.activities.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".views.activities.GroupChatActivity"
            android:exported="false" />
        <activity
            android:name=".views.activities.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".views.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/splashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.MainActivity"
            android:exported="false" />
    </application>

</manifest>